# Pinned to the exact versions the reported results were produced with.
# torch is the CUDA 12.8 build; on CPU-only machines install the CPU wheel
# of the same version (see README).
torch==2.11.0
torchvision==0.26.0
timm==1.0.28
numpy==2.5.2
scikit-learn==1.9.0
pillow==12.3.0
